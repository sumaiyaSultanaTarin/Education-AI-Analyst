# Phase 1 — FastAPI scaffold, LangGraph state/Supervisor stub, OpenRouter client.
# Extend per-phase as agents/tools land (see docs/final-task-assignment.md).

fastapi==0.141.1
uvicorn[standard]==0.52.3
pydantic-settings==2.15.0
python-dotenv==1.2.2

langgraph==1.2.11
langchain-core==1.5.4
openai==3.0.0
tenacity==9.1.4

# Persists the report/HITL graph's checkpoints to data/checkpoints.db so a
# session paused at the hitl_approval interrupt() survives a backend
# restart instead of losing its "still paused" state (see api/report_pipeline.py).
langgraph-checkpoint-sqlite==3.1.1

pytest==9.1.1
httpx==0.28.1

# Sample data pack generation (scripts/generate_sample_data.py) —
# also used by the Document Ingestion / Vision-OCR agents in Phase 2.
openpyxl==3.1.5
python-docx==1.2.0
fpdf2==2.8.8
Pillow==12.3.0

# Phase 2 — Document Ingestion Agent
pdfplumber==0.11.10
python-pptx==1.0.2

# Phase 2 — /sessions/{id}/documents upload endpoint (FastAPI UploadFile)
python-multipart==0.0.32

# Phase 3 — Knowledge/RAG Agent: local embeddings (OpenRouter doesn't serve
# embeddings, see CLAUDE.md) + Chroma vector store
sentence-transformers==3.3.1
chromadb==0.5.23

# Phase 3 — Data Analyst Agent: pandas over ingested XLSX rows
pandas==2.2.3

# Verify these three pins against what's actually resolvable in each
# teammate's environment before merging — added without a live install here.
